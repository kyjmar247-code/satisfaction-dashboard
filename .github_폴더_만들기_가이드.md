# .github 폴더 만들기 (GitHub 웹에서)

`.github` 폴더는 숨김 폴더라서 직접 업로드가 안 될 수 있습니다.
GitHub 웹에서 직접 만들어야 합니다!

## 📋 단계별 방법

### 1단계: workflows 폴더 만들기

1. GitHub 리포지토리 페이지에서 **"Add file"** → **"Create new file"** 클릭
2. 파일 경로에 입력: `.github/workflows/deploy.yml`
   - 이렇게 입력하면 `.github` 폴더와 `workflows` 폴더가 자동으로 생성됩니다!
3. 아래 파일 내용을 복사해서 붙여넣기
4. **"Commit new file"** 클릭

### 2단계: 파일 내용 복사

아래 내용을 그대로 복사해서 붙여넣으세요:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

## ✅ 완료!

이제 `.github/workflows/deploy.yml` 파일이 생성되었습니다!

